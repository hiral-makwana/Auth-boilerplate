export default interface userAttributes {
    id?: number,
    firstName?: string,
    email?: string,
    password?: string,
    status?: string,
    isVerified?: boolean,
    roleId?: number,
    profileImage?: string,
    isDeleted?: boolean,
}